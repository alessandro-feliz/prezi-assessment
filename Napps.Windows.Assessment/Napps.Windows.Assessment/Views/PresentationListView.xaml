<UserControl x:Class="Napps.Windows.Assessment.Views.PresentationListView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:Napps.Windows.Assessment.Views"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:views="clr-namespace:Napps.Windows.Assessment.Views"
             xmlns:cal="http://www.caliburnproject.org"
             xmlns:res="clr-namespace:Napps.Windows.Assessment.Properties"
             d:DesignHeight="450"
             d:DesignWidth="800"
             mc:Ignorable="d">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Page title and instruction -->
        <StackPanel Grid.Row="0">
            <TextBlock Text="{x:Static res:Resources.PresentationListTitle}" Style="{StaticResource Title}"/>
            <TextBlock Text="{x:Static res:Resources.PresentationListInstructions}" Style="{StaticResource SubTitle}"/>
        </StackPanel>

        <!-- Scrollable presentations list -->
        <ListBox Grid.Row="1"
                 ItemsSource="{Binding Presentations}" 
                 SelectedItem="{Binding SelectedPresentation, Mode=TwoWay}"
                 HorizontalAlignment="Stretch"
                 BorderThickness="0"
                 ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                 ScrollViewer.VerticalScrollBarVisibility="Auto"
                 ItemContainerStyle="{StaticResource PresentationItemCleanStyle}">

            <ListBox.ItemsPanel>
                <ItemsPanelTemplate>
                    <WrapPanel Orientation="Horizontal" HorizontalAlignment="Center" />
                </ItemsPanelTemplate>
            </ListBox.ItemsPanel>

            <ListBox.ItemTemplate>
                <DataTemplate>
                    <views:PresentationListItemView Margin="10" DataContext="{Binding}" />
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>
    </Grid>
</UserControl>