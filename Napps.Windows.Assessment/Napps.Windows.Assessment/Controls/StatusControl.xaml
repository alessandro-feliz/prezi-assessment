<UserControl x:Class="Napps.Windows.Assessment.Controls.StatusControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Napps.Windows.Assessment.Controls"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Border Margin="4" HorizontalAlignment="Right">
        <StackPanel Orientation="Horizontal" VerticalAlignment="Center" >

            <!-- Message -->
            <TextBlock Text="{Binding Message}" Style="{StaticResource StatusLabel}"/>

            <!-- Color Display -->
            <Ellipse Width="12" Height="12"
                     Margin="6,0,6,0"
                     Fill="{Binding ProgressStatus, Converter={StaticResource ProgressStatusEnumToBrushConverter}}"
                     Stroke="Transparent" StrokeThickness="0.5" />

        </StackPanel>
    </Border>
</UserControl>